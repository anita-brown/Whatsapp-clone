import express from 'express';
import app from '../app';

const router = express.Router();

// router


// get route
router.route("/").get();


// posts route
router.route("/").post();







// exported router
export default router;
